# 📚 نظام إدارة الدروس التعليمي - ZaEdu

نظام متكامل لإدارة الدروس والاختبارات والمواد الدراسية بالعربية.

---

## ⚙️ المتطلبات

- Python 3.8+
- Django 5.2.5
- PostgreSQL (للإنتاج) أو SQLite (للتطوير)

---

## 🚀 البدء السريع

### 1. **التثبيت**

```bash
# استنساخ المشروع
git clone <repository-url>
cd zadedu

# إنشاء بيئة افتراضية
python -m venv venv
source venv/bin/activate  # على Windows: venv\Scripts\activate

# تثبيت المتطلبات
pip install -r requirements.txt
```

### 2. **إعداد قاعدة البيانات**

```bash
# تطبيق الهجرات
python manage.py migrate

# إنشاء حساب مسؤول
python manage.py createsuperuser
```

### 3. **تشغيل الخادم**

```bash
python manage.py runserver
```

زُر `http://127.0.0.1:8000` في متصفحك.

---

## 📋 المميزات الرئيسية

| المميزة | الوصف |
|---------|--------|
| 👥 **متعدد المستخدمين** | كل معلم له سنواته ومواده الخاصة |
| 📖 **إدارة الدروس** | إنشاء وتحرير وحذف الدروس بسهولة |
| ❓ **نظام الأسئلة** | أسئلة درسية وأسبوعية (اختيارات متعددة) |
| 📅 **جدول المواد** | تحديد أيام تدريس كل مادة |
| 🧪 **الاختبارات** | اختبارات أسبوعية وامتحانات نهائية |
| 🔒 **آمن** | تسجيل دخول إجباري ومصادقة قوية |

---

## 🗂️ هيكل المشروع

```
zadedu/
├── manage.py              # أداة Django
├── requirements.txt       # المتطلبات
├── db.sqlite3            # قاعدة البيانات (محلي)
│
├── study_site/           # إعدادات المشروع
│   ├── settings.py       # الإعدادات الرئيسية
│   ├── urls.py          # المسارات
│   └── middleware.py    # البرامج الوسيطة
│
├── edu/                  # تطبيق التعليم
│   ├── models.py        # نماذج البيانات
│   ├── views.py         # العروض (Views)
│   ├── forms.py         # النماذج (Forms)
│   ├── admin.py         # لوحة الإدارة
│   └── migrations/      # هجرات قاعدة البيانات
│
├── templates/           # قوالب HTML
│   ├── base.html       # القالب الأساسي
│   ├── select_year_term.html
│   ├── term_subjects.html
│   ├── lesson_detail.html
│   └── registration/   # قوالب التسجيل
│
└── static/             # الملفات الثابتة
    └── app.css         # أنماط CSS
```

---

## 🔗 المسارات الرئيسية

| المسار | الوصف |
|--------|--------|
| `/` | الصفحة الرئيسية واختيار السنة والفصل |
| `/accounts/login/` | تسجيل الدخول |
| `/accounts/register/` | التسجيل (مستخدم جديد) |
| `/term/<id>/` | شبكة المواد للفصل |
| `/term/<id>/subject/<id>/lessons/` | دروس المادة |
| `/term/<id>/subject/<id>/weekly/` | الاختبار الأسبوعي |
| `/admin/` | لوحة الإدارة |

---

## 📊 نماذج البيانات

- **Year**: السنة الدراسية (سنة أولى، سنة ثانية)
- **Term**: الفصل الدراسي (فصل أول، فصل ثاني)
- **Subject**: المادة الدراسية
- **Lesson**: الدرس داخل المادة
- **Question**: الأسئلة والاختيارات
- **WeeklyQuiz**: الاختبارات الأسبوعية

---

## ⚙️ الإعدادات البيئية

أنشئ ملف `.env` في جذر المشروع:

```env
DEBUG=1
SECRET_KEY=your-secret-key-here
DJANGO_ENV=development
DATABASE_URL=postgresql://user:password@localhost/zadedu
ALLOWED_HOSTS=127.0.0.1,localhost
```

---

## 🔧 أوامر مفيدة

```bash
# إنشاء نموذج جديد
python manage.py makemigrations

# تطبيق الهجرات
python manage.py migrate

# إنشاء مسؤول جديد
python manage.py createsuperuser

# تجميع الملفات الثابتة
python manage.py collectstatic

# اختبار المشروع
python manage.py test
```

---

## 📝 الترخيص

جميع الحقوق محفوظة © 2026

---

## 💡 الملاحظات

- المشروع يدعم اللغة العربية بالكامل
- تم تكييفه مع المناهج التعليمية السعودية
- يدعم التطوير المحلي والإنتاج
- مُحسَّن للأداء مع WhiteNoise و PostgreSQL
